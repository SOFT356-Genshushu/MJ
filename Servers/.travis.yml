language: node_js
node_js: 
    - '12'
cache:
  directories:
    - node_modules

install:
  - npm install

before_script:
script:
  - npm run build

after_script:
  - cd ./dist
  - git init
  - git config user.name "SOFT356-Genshushu"
  - git config user.email "zhuang.ma@students.plymouth.ac.uk"
  - git add .
  - git commit -m "Update tools"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:${P_BRANCH}

branches:
  only:
    - master